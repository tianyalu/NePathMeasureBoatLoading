<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <com.sty.ne.boat.loading.view.RippleAnimationView
        android:id="@+id/ripple_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        app:radius="150"
        app:ripple_anim_color="@color/rippleColor"
        app:ripple_anim_type="fillRipple"
        app:strokeWidth="18">
        <ImageView
            android:id="@+id/image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@drawable/music"/>
    </com.sty.ne.boat.loading.view.RippleAnimationView>

    <TextView
        android:id="@+id/tv_recognize_music"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:text="点击识别音乐"
        android:textSize="14sp"/>

    <TextView
        android:id="@+id/tv_recognize_song"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:textColor="@android:color/darker_gray"
        android:text="听歌识曲，识别你周围播放的歌"
        android:textSize="12sp"/>
</LinearLayout>